@model List<TodoItem>

<h2>Todos</h2>

<table class="table table-dark table-hover">
    <tr>
        <th>ID</th>
        <th>Name</th>
    </tr>


    @foreach (TodoItem item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
        </tr>
    }

</table>